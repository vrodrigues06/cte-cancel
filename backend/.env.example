# Banco de dados (Postgres externo)
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# URL base do SAP (placeholder, ajuste depois)
SAP_BASE_URL="https://sap.example.com/api/authorizations"

# Origem autorizada para CORS (frontend)
ALLOWED_ORIGIN="http://localhost:3000"
